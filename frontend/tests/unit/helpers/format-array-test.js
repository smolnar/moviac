import { formatArray } from '../../../helpers/format-array';
import { module, test } from 'qunit';

module('Unit | Helper | format array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatArray(42);
  assert.ok(result);
});
